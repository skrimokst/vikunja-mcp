# Python
__pycache__/
*.py[cod]
.venv/
.pytest_cache/

# OS / editor cruft
Thumbs.db
.DS_Store
*.swp

# Built MCP bundles — a local build artifact (may carry a machine-specific launcher path); never commit
*.mcpb
